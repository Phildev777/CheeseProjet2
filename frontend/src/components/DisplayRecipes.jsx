/* import React from "react";

function DisplayRecipes({ recipe }) {
  return (
    recipes && (
      <div className="DisplayRecipes">
        <img src={recipe.recipeImg} alt={recipe.recipeName} />
        <ul>
          <li>Name: {recipe.recipeName}</li>
          <li>Cheese Used: {recipe.cheeseUsed}</li>
          <li>Stages : {recipe.ingredients}</li>
          <li>Video : {recipe.recipeLink}</li>
        </ul>
      </div>
    )
  );
}

export default DisplayRecipes; */
